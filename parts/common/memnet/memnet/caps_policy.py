"""CapsPolicy — SysML alias for config.Caps (size caps + shipped ACL flags)."""

from __future__ import annotations

from memnet.config import Caps

CapsPolicy = Caps

__all__ = ["CapsPolicy", "Caps"]
