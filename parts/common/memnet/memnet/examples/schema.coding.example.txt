# Session map for session_open --map-file (SCHEMA lines).
# Coding kinds for workflow.coding.example.txt. Prefer SCHEMA; leftover @TAG pipe still loads.
# Built-in LAW / EDG are merged automatically — do not redefine them here.
SCHEMA CFG ; fields=id repo anchor version notes
SCHEMA MOD ; fields=id path summary status recycle
SCHEMA SYM ; fields=id name kind path line signature status recycle
SCHEMA TSK ; fields=id goal anchor status recycle
SCHEMA USR ; fields=id topic content status recycle
SCHEMA DEC ; fields=id task question options chosen recycle
