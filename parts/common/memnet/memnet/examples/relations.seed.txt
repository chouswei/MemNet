# Default relationship types loaded on every session_open (_seed_relations).
# Add further verbs with leftover add/update --allow-new-relation, or mutate CREATE of a new type when allowed.
# Agent wire is GQL relationship types — not Layer ASCII, not @EDG pipe teach.

# Demo world (workflow.example.txt)
seeks_help
binds
produces
links

# Bind / chart (GQL :bind plus other rel types)
bind
pipe
knows
reports_to
member_of

# Coding / doctrine (workflow.coding.example.txt)
owns
contains
defines
implements
calls
includes
uses
on_net
tests
documents
satisfies
constrained_by
next
delegates
overrides
governs
paired_with
triggers
preempts
memberOf
mentions
maps_to
helps
