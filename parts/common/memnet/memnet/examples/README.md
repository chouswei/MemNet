# Bundled examples under parts/common/memnet/memnet/examples/
#
# Classification (British English; align with docs/grammar/):
#
# TagMap / session_open (pipe field maps — NOT agent mutate):
#   schema.example.txt          — demo world map (tests + quick start)
#   schema.coding.example.txt   — coding map (MOD/SYM/TSK/…)
#   schema.techdocs.example.txt — tech-docs / SCPI map
#
# Relation seed (session_open vocabulary):
#   relations.seed.txt          — default @EDG verbs (_seed_relations)
#
# Agent-facing workflow seeds (Tier A Write=display mutate ops):
#   workflow.example.txt        — demo world seed (stable ids for tests)
#   workflow.coding.example.txt — coding tutorial seed
#
# Machine / durable pipe seeds (legacy @TAG; not teaching agents to goldfish-dump):
#   workflow.memnet-codebase.snap.txt — regenerated by scripts/generate_memnet_codebase_seed.py
#   workflow.rto-remote.example.txt   — RTO SCPI dictionary; scripts/extract_rto_scpi.py
#
# Agent grammar fixtures live in docs/grammar/examples/ (pin map bare present; mutate with +/~/-).
# CLI: memnet examples path | map | workflow | agent-guide
