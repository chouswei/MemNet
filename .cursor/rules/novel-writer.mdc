---
description: MemNet novel — Cursor chat is DEBUG only. Production = novel-mobile UI + cursor_beat pipeline.
alwaysApply: false
---

# Novel writer — Cursor chat (debug only)

**Production play:** [`applications/novel_mobile/`](../applications/novel_mobile/README.md) — LAN SPA for 【神域】setup + 【劇情】beats. Same `session_id.txt` and `play_service.run_beat` as CLI.

**This chat:** inspect graph, smoke setup FSM, shell `cursor_beat.py`, or ad-hoc MCP — **not** the player-facing loop.

Docs: [`application-notes/llm-novel-cursor-sdk.md`](application-notes/llm-novel-cursor-sdk.md). Instance example: `shenjia_caifa`.

## Debug — graph / setup FSM

- `read_player_setup(session)` — see `setup_guidance.next_action` (mirrors mobile `/api/setup`)
- Setup commits: `commit_player_profile`, `commit_opening_pick`, `commit_setup_ack` (same as mobile `/api/setup/*`)
- `read_get`, `query warm`, `session_save` / `session_load`
- Do **not** invent god-realm copy — seed `setup_*` USR rows are SSOT

## Debug — beat pipeline

- One shell per test beat:

  `python applications/novel_cursor/cursor_beat.py --app <slug> --choice N`

- Show `NOVEL_BEAT_RESULT` only when reproducing CLI output
- **Forbidden:** hand-draft `@OLN`/`@SBD`/`@SCR`/prose and call `beat_turn_finish` from chat (system tests only)
- `beat_turn_*` / `script_beat_prepare` / `prose_beat_prepare` — orchestrator internals; not operator play
- **World lore in seed only:** 魂穿、靈魂圖書館、文風、選項槽語意等 → seed `@USR`/`@LAW`/`@GLO`；`presentation.contracts` 注入 prompt。禁在 `beat_prompt.py` 等應用層 hardcode 世界設定。

## Prerequisites

- `memnet serve`; `DEEPSEEK_API_KEY` (or `LLM_API_KEY_*`)
- `novel-output/<slug>/session_id.txt` after `novel_bootstrap.py --app <slug>`
