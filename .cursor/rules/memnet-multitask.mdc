---
name: memnet-multitask
description: >-
  Cursor Multitask Mode or Task sub-agents with MemNet — thin project reminder.
  Application skill: ~/.cursor/skills/memnet-multitask/. Product ops doc:
  docs/multi-agent-sessions.md.
alwaysApply: false
---

# MemNet + Multitask Mode

Load user-pack skill **`memnet-multitask`** (`~/.cursor/skills/memnet-multitask/SKILL.md`). Product ops SSOT: `docs/multi-agent-sessions.md`. System-repo pattern: `docs/application-notes/llm-system-dev-multitask.md`. Chat is never mission SSOT.

## MUST

- One shared session id; **TCP serve** or **streamable-http** MCP (not in-process stdio).
- Parent: mint/settle `TSK_*` / `USR_*`; end turn after delegate; `pin_map` on next coordinator turn.
- Workers: `pin_map` first; mutate only under assigned subgraph.

## MUST NOT

- Treat chat as durable state; poll workers; assume ACL / reserve / PinMapIngest_* (0.4.x design-only).
