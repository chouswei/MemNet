"""Optional era display formatters registered via time_display.register_formatter.

Shipped examples (e.g. chongzhen_shichen) are **seed opt-in** via @USR game_time —
not required for every novel project.
"""
