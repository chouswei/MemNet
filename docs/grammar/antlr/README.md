# ANTLR — historical note

Layer `MemNetLayer.g4` sources were **quarantined** to [`../archive/antlr/`](../archive/antlr/).

**1.x agent teach** is GQL only: [`../gql-wire-profile.md`](../gql-wire-profile.md).  
R1 stub [`../MemNet.g4`](../MemNet.g4) remains for as-is shared-dialect harness continuity until M2 retires that codec path — **not** a second agent teach dialect.
