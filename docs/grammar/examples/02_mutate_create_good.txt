# expect: parse-ok
# Create nodes with mint [NEW]; engine allocates real ids.
# Do not invent ground ids. Link with edges only after ids are known (warm / response).
+ CLM [NEW] ; type=fact ; code=KCL residual sums branch currents ; recycle=persistent
+ CLM [NEW] ; type=decision ; code=use derives self-loop for Ohm ; recycle=persistent
+ NEW [TSK_inv] --(part_of)--> [MOD_memnet] ; note=pending_claim_link ; recycle=delete_on_settle
